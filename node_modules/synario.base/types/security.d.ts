export declare function uuid(): string;
export declare function randomNum(): number;
export declare function encode(str: string): Uint8Array;
export declare function decode(buffer: ArrayBuffer, format?: string): string;
export declare class EcdhSecurity {
    generateIv(): Uint8Array;
    private key?;
    private sessionkey?;
    private generateKey;
    exportKey(): Promise<Uint8Array>;
    importKey(keyobj: Uint8Array): Promise<void>;
    encrypt(plaintext: string): Promise<{
        buffer: ArrayBuffer;
        iv: Uint8Array;
    }>;
    decrypt(data: {
        buffer: ArrayBuffer;
        iv: Uint8Array;
    }): Promise<string>;
    private setRemotePublicKey;
}
